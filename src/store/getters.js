const getters = {
  getCouter(state) {
    return state.couter;
  },
  getLanguge(state) {
    return state.language;
  },
  getUsers(state) {
    return state.users;
  },
};

export default getters;
